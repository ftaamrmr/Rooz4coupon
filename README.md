# Rooz4coupon - موقع كوبونات وعروض

## 📊 تقرير حالة المشروع

### ✅ ما تم إنجازه حتى الآن

#### 1. البنية الأساسية (100% مكتمل)
- ✅ ملف الدخول الرئيسي `index.php`
- ✅ نظام التوجيه (Router) مع دعم URLs صديقة لمحركات البحث
- ✅ ملفات التكوين (`config/config.php`, `config/db.php`)
- ✅ ملف `.htaccess` للـ Apache مع:
  - إعادة توجيه URLs
  - حماية أمنية
  - ضغط الملفات
  - التخزين المؤقت للمتصفح

#### 2. قاعدة البيانات (100% مكتمل)
- ✅ `database.sql` يحتوي على جميع الجداول:
  - `users` - المستخدمين (admin, editor, writer)
  - `categories` - الفئات (ثنائي اللغة)
  - `stores` - المتاجر
  - `coupons` - الكوبونات
  - `coupon_categories` - ربط الكوبونات بالفئات
  - `articles` - المقالات (نظام المدونة)
  - `article_categories` - فئات المقالات
  - `tags` - الوسوم
  - `article_tags` - ربط المقالات بالوسوم
  - `settings` - الإعدادات
  - `subscribers` - المشتركين في النشرة
  - `pages` - الصفحات الثابتة
- ✅ بيانات تجريبية جاهزة

#### 3. Controllers (100% مكتمل)
- ✅ `BaseController.php` - الكلاس الأساسي
- ✅ `HomeController.php` - الصفحة الرئيسية
- ✅ `StoreController.php` - المتاجر
- ✅ `CategoryController.php` - الفئات
- ✅ `CouponController.php` - الكوبونات
- ✅ `ArticleController.php` - المقالات/المدونة
- ✅ `SearchController.php` - البحث
- ✅ `PageController.php` - الصفحات الثابتة
- ✅ `SeoController.php` - Sitemap & Robots.txt
- ✅ `SubscribeController.php` - الاشتراك في النشرة

#### 4. Views الواجهة الأمامية (100% مكتمل)
- ✅ `partials/header.php` - الهيدر الرئيسي
- ✅ `partials/footer.php` - الفوتر
- ✅ `home.php` - الصفحة الرئيسية
- ✅ `stores.php` - قائمة المتاجر
- ✅ `store-single.php` - صفحة المتجر
- ✅ `categories.php` - الفئات
- ✅ `category-single.php` - صفحة الفئة
- ✅ `coupons.php` - الكوبونات
- ✅ `coupons-expired.php` - الكوبونات المنتهية
- ✅ `blog.php` - المدونة
- ✅ `article-single.php` - صفحة المقال
- ✅ `search.php` - نتائج البحث
- ✅ `page.php` - الصفحات الثابتة
- ✅ `404.php` - صفحة الخطأ

#### 5. الميزات المضمنة
- ✅ **ثنائي اللغة**: دعم كامل للعربية والإنجليزية
- ✅ **RTL/LTR**: تبديل تلقائي حسب اللغة
- ✅ **الوضع المظلم**: Dark Mode
- ✅ **تصميم متجاوب**: Responsive Design باستخدام TailwindCSS
- ✅ **SEO محسن**: Meta tags, Open Graph, Schema.org
- ✅ **CSRF Protection**: حماية ضد هجمات CSRF
- ✅ **نظام النشرة البريدية**: اشتراك البريد الإلكتروني
- ✅ **نسخ الكود**: وظيفة نسخ كود الكوبون
- ✅ **العد التنازلي**: عرض وقت انتهاء الكوبون
- ✅ **Sitemap.xml & Robots.txt**: توليد تلقائي

#### 6. Helper Functions
- ✅ `functions.php` مع دوال مساعدة شاملة

---

### ⚠️ ما ينقص المشروع

#### 1. لوحة التحكم (Admin Panel) - غير موجودة
- ❌ إدارة المستخدمين
- ❌ إدارة المتاجر
- ❌ إدارة الكوبونات
- ❌ إدارة الفئات
- ❌ إدارة المقالات
- ❌ إدارة الإعدادات
- ❌ رفع الصور

#### 2. مجلد Public - غير موجود
- ❌ مجلد `public/` للملفات العامة
- ❌ مجلد `public/uploads/` لرفع الصور
- ❌ ملفات CSS/JS مخصصة (يستخدم CDN حالياً)

#### 3. صفحات مفقودة
- ❌ `coupon-single.php` - صفحة تفاصيل الكوبون
- ❌ `blog-category.php` - فئات المدونة
- ❌ `blog-tag.php` - وسوم المدونة

---

### 🚀 خطوات الرفع على استضافة مشتركة

#### المتطلبات:
- PHP 7.4+ (يُفضل PHP 8.0+)
- MySQL 5.7+ أو MariaDB 10.3+
- Apache مع mod_rewrite

#### خطوات التثبيت:

1. **رفع الملفات**
   ```
   - ارفع جميع الملفات إلى مجلد public_html
   ```

2. **إنشاء قاعدة البيانات**
   ```sql
   -- من لوحة تحكم الاستضافة (cPanel):
   -- 1. أنشئ قاعدة بيانات جديدة
   -- 2. أنشئ مستخدم لقاعدة البيانات
   -- 3. اربط المستخدم بقاعدة البيانات
   -- 4. استورد ملف database.sql
   ```

3. **تعديل إعدادات قاعدة البيانات**
   ```php
   // في ملف config/db.php
   define('DB_HOST', 'localhost');
   define('DB_NAME', 'اسم_قاعدة_البيانات');
   define('DB_USER', 'اسم_المستخدم');
   define('DB_PASS', 'كلمة_المرور');
   ```

4. **إنشاء المجلدات المطلوبة**
   ```bash
   mkdir -p public/uploads/images
   mkdir -p public/uploads/logos
   chmod 755 public/uploads -R
   ```

5. **تعديل إعدادات الموقع**
   ```php
   // في ملف config/config.php
   // غيّر هذا السطر للإنتاج:
   error_reporting(0);
   ini_set('display_errors', 0);
   ```

6. **تفعيل HTTPS** (موصى به)
   ```apache
   # في ملف .htaccess، أزل التعليق من:
   RewriteCond %{HTTPS} off
   RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]
   ```

---

### 📋 بيانات الدخول الافتراضية

```
Username: admin
Email: admin@example.com
Password: admin123
```

**⚠️ هام: غيّر كلمة المرور فوراً بعد التثبيت!**

---

### 📁 هيكل المشروع

```
Rooz4coupon/
├── index.php                 # نقطة الدخول الرئيسية
├── .htaccess                 # إعدادات Apache
├── database.sql              # مخطط قاعدة البيانات
├── README.md                 # هذا الملف
├── config/
│   ├── config.php           # إعدادات التطبيق
│   └── db.php               # اتصال قاعدة البيانات
└── app/
    ├── router.php           # نظام التوجيه
    ├── controllers/         # المتحكمات
    │   ├── BaseController.php
    │   ├── HomeController.php
    │   ├── StoreController.php
    │   ├── CategoryController.php
    │   ├── CouponController.php
    │   ├── ArticleController.php
    │   ├── SearchController.php
    │   ├── PageController.php
    │   ├── SeoController.php
    │   └── SubscribeController.php
    ├── helpers/
    │   └── functions.php    # دوال مساعدة
    └── views/
        └── frontend/
            ├── partials/
            │   ├── header.php
            │   └── footer.php
            ├── home.php
            ├── stores.php
            ├── store-single.php
            ├── categories.php
            ├── category-single.php
            ├── coupons.php
            ├── coupons-expired.php
            ├── blog.php
            ├── article-single.php
            ├── search.php
            ├── page.php
            └── 404.php
```

---

### 🔧 تقنيات مستخدمة

- **PHP 7.4+** - اللغة الرئيسية
- **MySQL/MariaDB** - قاعدة البيانات
- **PDO** - للاتصال الآمن بقاعدة البيانات
- **TailwindCSS (CDN)** - التصميم
- **Font Awesome** - الأيقونات
- **Google Fonts** - الخطوط (Cairo للعربية, Inter للإنجليزية)

---

### 📊 نسبة الإنجاز

| القسم | النسبة |
|-------|--------|
| الواجهة الأمامية (Frontend) | 95% |
| قاعدة البيانات | 100% |
| نظام التوجيه | 100% |
| SEO | 100% |
| الأمان الأساسي | 85% |
| لوحة التحكم (Admin) | 0% |
| **الإجمالي** | **~70%** |

---

### 🎯 التوصيات

1. **لوحة التحكم ضرورية** - بدونها لا يمكن إدارة المحتوى
2. **إضافة الصفحات المفقودة** - coupon-single, blog-category, blog-tag
3. **إنشاء مجلد public** مع الملفات الثابتة
4. **اختبار على خادم محلي** قبل الرفع للاستضافة

---

### 📝 ملاحظة

هذا المشروع جاهز للعرض الأمامي (Frontend) ولكنه يحتاج لوحة تحكم لإدارة المحتوى. يمكن رفعه على استضافة مشتركة واستخدام phpMyAdmin لإدارة البيانات مؤقتاً حتى يتم بناء لوحة التحكم.

---

## License

MIT License